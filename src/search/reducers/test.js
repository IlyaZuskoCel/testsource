/**
 * Created by kirill on 8/21/17.
 * moonion.com
 */

function current(state = null, action) {
    switch (action.type) {
        default:
            return state;
    }
}

export default current;
